clc;
clear all;
f0=input ('enter the frequency');
fs = 20*f0;
t=0:1/fs:2/f0;
x = sin(2*pi*f0*t);
subplot(211)
plot(t,x)


%frequency spectrum
Xk = fft(x,256);
magX = abs(Xk);
w=linspace(0, fs/2, length(x)/2);
subplot(212)
plot(w, magX(1:length(x)/2))